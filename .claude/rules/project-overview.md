## Project Overview

A Rust playground/experimentation project using [GPUI](https://github.com/zed-industries/zed/tree/main/crates/gpui), the GPU-accelerated UI framework from the Zed editor. Uses Rust edition 2024. GPUI is pulled directly from the Zed monorepo via git dependency (unpinned, tracks latest).
